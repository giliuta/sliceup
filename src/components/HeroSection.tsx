"use client";

// TODO: Main product showcase
// - Full-screen 100vh
// - Background text (huge, opacity 0.08)
// - Product video with mix-blend-mode: screen
// - Product info left (name, subtitle, description, Shop Now)
// - Price right
// - Mouse parallax on video + bg text
// - Staggered fadeUp animations on load
export default function HeroSection() {
  return <section className="relative h-screen w-full overflow-hidden" />;
}
