"use client";

// TODO: Minimal floating navigation
// - Logo left
// - Menu links center/right
// - Cart icon with badge
// - Fixed position, transparent bg with backdrop blur
export default function Nav() {
  return <nav className="fixed top-0 left-0 right-0 z-50 px-12 py-6" />;
}
