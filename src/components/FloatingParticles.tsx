"use client";

// TODO: Ambient floating particles
// - Small circles/dots with random positions
// - Float animation with varying speeds
// - Color tinted to match current product theme
// - Parallax at different speeds per particle
export default function FloatingParticles() {
  return <div className="pointer-events-none fixed inset-0 z-0" />;
}
