"use client";

// TODO: Product thumbnail selector
// - Horizontal row of thumbnails at bottom
// - Click → switch active product (triggers theme change)
// - Keyboard arrow navigation
// - Swipe on mobile
// - Active thumbnail has border highlight
export default function ProductSwitcher() {
  return <div className="absolute bottom-8 left-0 right-0" />;
}
